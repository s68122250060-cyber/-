# ข้อ 3: การเปรียบเทียบจำนวนสระและพยัญชนะ

## 1. คำอธิบายแนวคิดของอัลกอริทึม

### เงื่อนไขและการจัดการข้อมูล (Constraints & Rules)
* สระภาษาอังกฤษ ได้แก่ `a, e, i, o, u` (ไม่แยกตัวพิมพ์เล็กและตัวพิมพ์ใหญ่)
* ตัวอักษรอื่นๆ ที่เป็นอักขระภาษาอังกฤษนับเป็น พยัญชนะ (Consonants)
* **ไม่นับ:** ตัวเลข, ช่องว่าง, และเครื่องหมายพิเศษทุกชนิด

### อัลกอริทึมที่ 1: Recursive Counting (`hasMoreVowelsRecursive`)
* **แนวคิด:** ใช้การประมวลผลแบบเวียนเกิดทีละตัวอักษร โดยส่งดัชนีตำแหน่งปัจจุบัน (`index`) พร้อมค่าสะสมนับสระ (`vowelCount`) และพยัญชนะ (`consonantCount`) ไปยังการเรียกเมธอดครั้งถัดไป
* **Base Case:** เมื่อดัชนี `index == s.length()` (ประมวลผลครบทุกตัวอักษรแล้ว) ให้ทำการเปรียบเทียบค่า `vowelCount > consonantCount` แล้ว คืนค่า `true` หรือ `false`
* **Recursive Case:** ตรวจสอบอักขระ ณ ตำแหน่ง `index`
  * หากเป็นสระ: บวกเพิ่ม `vowelCount + 1`
  * หากเป็นพยัญชนะ: บวกเพิ่ม `consonantCount + 1`
  * หากเป็นอักขระอื่นๆ (ตัวเลข/ช่องว่าง/เครื่องหมาย): ไม่เพิ่มค่าใดๆ
  * เรียกเมธอดซ้ำโดยส่ง `index + 1` พร้อมค่าตัวนับที่ปรับปรุงแล้ว

### อัลกอริทึมที่ 2: Iterative Counting (`hasMoreVowelsIterative`)
* **แนวคิด:** ใช้ลูป `for` วนอ่านอักขระในสตริงตั้งแต่ตำแหน่งแรกถึงตำแหน่งสุดท้าย มีตัวแปรนับสระและพยัญชนะท้องถิ่น ตรวจสอบประเภทอักขระและเพิ่มค่าตัวนับ จากนั้นเปรียบเทียบผลลัพธ์เมื่อวนลูปจบ

---

## 2. Pseudocode

```text
// เมธอดช่วยตรวจสอบว่าเป็นสระหรือไม่
Algorithm isVowel(ch)
    Convert ch to lowercase
    Return true if ch is 'a', 'e', 'i', 'o', 'u'
End Algorithm

// เมธอดช่วยตรวจสอบว่าเป็นพยัญชนะหรือไม่
Algorithm isConsonant(ch)
    Convert ch to lowercase
    Return true if ch is 'a'-'z' and not isVowel(ch)
End Algorithm

// 1. Recursive Counting Algorithm (Helper)
Algorithm countRecursive(s, index, vowelCount, consonantCount)
    If index == length of s Then
        Return vowelCount > consonantCount     // Base Case: ตรวจครบแล้ว คืนผลเปรียบเทียบ
    End If
    
    ch = character at s[index]
    If isVowel(ch) Then
        vowelCount = vowelCount + 1
    Else If isConsonant(ch) Then
        consonantCount = consonantCount + 1
    End If
    
    Return countRecursive(s, index + 1, vowelCount, consonantCount) // Recursive Case
End Algorithm

Algorithm hasMoreVowelsRecursive(s)
    If s is null Then Return false
    Return countRecursive(s, 0, 0, 0)
End Algorithm

// 2. Iterative Counting Algorithm
Algorithm hasMoreVowelsIterative(s)
    If s is null Then Return false
    vowelCount = 0
    consonantCount = 0
    For i = 0 To length of s - 1 Do
        ch = character at s[i]
        If isVowel(ch) Then
            vowelCount = vowelCount + 1
        Else If isConsonant(ch) Then
            consonantCount = consonantCount + 1
        End If
    End For
    Return vowelCount > consonantCount
End Algorithm
```
## 3. ภาษา Java
```text
public class VowelConsonantCounter {

    // เมธอดช่วยตรวจสอบอักขระว่าเป็นสระหรือไม่ (ไม่แยกพิมพ์เล็ก-ใหญ่)
    private static boolean isVowel(char c) {
        char lower = Character.toLowerCase(c);
        return lower == 'a' || lower == 'e' || lower == 'i' || lower == 'o' || lower == 'u';
    }

    // เมธอดช่วยตรวจสอบอักขระว่าเป็นพยัญชนะหรือไม่ (ไม่นับตัวเลข/ช่องว่าง/เครื่องหมาย)
    private static boolean isConsonant(char c) {
        char lower = Character.toLowerCase(c);
        return lower >= 'a' && lower <= 'z' && !isVowel(lower);
    }

    /**
     * อัลกอริทึมที่ 1: Recursive Counting (เมธอดช่วยเวียนเกิด)
     * Base Case: index == s.length() คืนค่าผลการเปรียบเทียบ
     * Recursive Case: เพิ่มค่าตัวนับ และส่ง index + 1
     */
    private static boolean countRecursive(String s, int index, int vowels, int consonants) {
        // Base Case: เมื่อประมวลผลจนจบสตริง
        if (index == s.length()) {
            return vowels > consonants;
        }

        char currentChar = s.charAt(index);
        if (isVowel(currentChar)) {
            vowels++;
        } else if (isConsonant(currentChar)) {
            consonants++;
        }
        // ไม่นับตัวเลข ช่องว่าง หรือเครื่องหมายพิเศษ

        // Recursive Case: ขยับตำแหน่ง index + 1
        return countRecursive(s, index + 1, vowels, consonants);
    }

    // เมธอดหลักสำหรับเรียกใช้ Recursive Counting
    public static boolean hasMoreVowelsRecursive(String s) {
        if (s == null) return false; // ดักจับ Null Pointer
        return countRecursive(s, 0, 0, 0);
    }

    /**
     * อัลกอริทึมที่ 2: Iterative Counting
     * ใช้ลูปวนอ่านตัวอักษรทีละตัว แล้วเพิ่มค่าตัวนับ
     */
    public static boolean hasMoreVowelsIterative(String s) {
        if (s == null) return false;

        int vowels = 0;
        int consonants = 0;

        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);
            if (isVowel(c)) {
                vowels++;
            } else if (isConsonant(c)) {
                consonants++;
            }
        }

        return vowels > consonants;
    }

    public static void main(String[] args) {
        // 1. ทดสอบกรณีปกติ
        String test1 = "education"; // Vowels: 5, Consonants: 4 -> true
        System.out.println("Input: \"" + test1 + "\"");
        System.out.println("  Recursive: " + hasMoreVowelsRecursive(test1));
        System.out.println("  Iterative: " + hasMoreVowelsIterative(test1));

        // 2. ทดสอบสตริงที่มีตัวเลข ช่องว่าง และเครื่องหมายพิเศษ
        String test2 = "Hello World 123!!!"; // Vowels: 3, Consonants: 7 -> false
        System.out.println("\nInput: \"" + test2 + "\"");
        System.out.println("  Recursive: " + hasMoreVowelsRecursive(test2));
        System.out.println("  Iterative: " + hasMoreVowelsIterative(test2));

        // 3. ทดสอบกรณีพิเศษ (Edge Cases)
        System.out.println("\n=== Testing Edge Cases ===");
        System.out.println("Null Input:   " + hasMoreVowelsIterative(null));
        System.out.println("Empty String: " + hasMoreVowelsRecursive(""));
        System.out.println("Numbers Only: " + hasMoreVowelsIterative("123456"));
    }
}
```
## 4. ข้อมูลนำเข้า&ออก
```
Input: "education"          -> Vowels: 5, Consonants: 4 -> Result: true
Input: "Hello World 123!!!" -> Vowels: 3, Consonants: 7 -> Result: false
Input: "aeiou"              -> Vowels: 5, Consonants: 0 -> Result: true
Input: "123456"             -> Vowels: 0, Consonants: 0 -> Result: false
```
