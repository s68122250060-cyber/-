# ข้อ 4: การจัดกลุ่มจำนวนคู่และจำนวนคี่

## 1. คำอธิบายแนวคิดของอัลกอริทึม

### โจทย์กำหนด
จัดตำแหน่งสมาชิกในอาร์เรย์จำนวนเต็ม $A$ ใหม่ โดยให้ **จำนวนคู่ทั้งหมดอยู่ก่อนจำนวนคี่** (ไม่จำเป็นต้องเรียงค่าภายในกลุ่มจากน้อยไปมาก)

### อัลกอริทึมที่ 1: Recursive Two-Pointer (`rearrangeRecursive`)
* **แนวคิด:** ใช้ตัวชี้ `left` (เริ่มจากตำแหน่ง 0) และ `right` (เริ่มจากตำแหน่งสุดท้าย) 
* **Base Case:** เมื่อ `left >= right` แสดงว่าตัวชี้ทั้งสองสแกนมาชนหรือสวนกันแล้ว ให้หยุดการทำงาน
* **Recursive Step / Swap Rule:**
  * หาก $A[left]$ เป็นเลขคู่ ให้ขยับ `left + 1`
  * หาก $A[right]$ เป็นเลขคี่ ให้ขยับ `right - 1`
  * หาก $A[left]$ เป็นเลขคี่ และ $A[right]$ เป็นเลขคู่ ให้ทำการ **สลับค่า (Swap)** ระหว่าง $A[left]$ กับ $A[right]$ แล้วขยับ `left + 1` และ `right - 1`
  * เรียกเมธอดเวียนเกิดซ้ำกับช่วงที่เหลือ

### อัลกอริทึมที่ 2: Iterative Two-Pointer (`rearrangeTwoPointer`)
* **แนวคิด:** ใช้หลักการ Two-Pointer เดียวกันกับวิธีแรก แต่เปลี่ยนมาใช้ลูป `while (left < right)` วนสลับข้อมูลแบบ In-place จนกว่าตัวชี้ซ้ายและขวาจะสวนกัน

### อัลกอริทึมที่ 3: Extra Array (`rearrangeExtraArray`)
* **แนวคิด:** สร้างอาร์เรย์ใหม่ขนาดเท่าเดิม สแกนอาร์เรย์เดิมรอบแรก ดึงเฉพาะ **เลขคู่** ใส่ลงอาร์เรย์ใหม่ จากนั้นสแกนรอบที่สอง ดึงเฉพาะ **เลขคี่** ใส่ต่อท้าย ซึ่งวิธีนี้จะช่วยรักษาลำดับดั้งเดิมของสมาชิกไว้ได้ (Stable Algorithm)

---

## 2. Pseudocode

```text
// 1. Recursive Two-Pointer Algorithm
Algorithm rearrangeRecursive(A, left, right)
    If left >= right Then
        Return                                // Base Case: ตัวชี้ชนหรือสวนกันแล้ว
    End If
    
    If A[left] % 2 == 0 Then
        rearrangeRecursive(A, left + 1, right) // ถ้าซ้ายเป็นคู่ ให้ขยับซ้าย
    Else If A[right] % 2 != 0 Then
        rearrangeRecursive(A, left, right - 1) // ถ้าขวาเป็นคี่ ให้ขยับขวา
    Else
        Swap A[left] and A[right]              // สลับถ้าซ้ายเป็นคี่และขวาเป็นคู่
        rearrangeRecursive(A, left + 1, right - 1)
    End If
End Algorithm

// 2. Iterative Two-Pointer Algorithm
Algorithm rearrangeTwoPointer(A)
    If A is null Then Return
    left = 0
    right = length of A - 1
    While left < right Do
        While left < right and A[left] % 2 == 0 Do
            left = left + 1
        End While
        While left < right and A[right] % 2 != 0 Do
            right = right - 1
        End While
        If left < right Then
            Swap A[left] and A[right]
            left = left + 1
            right = right - 1
        End If
    End While
End Algorithm

// 3. Extra Array Algorithm (Stable)
Algorithm rearrangeExtraArray(A)
    If A is null Then Return null
    Create new array result of same size as A
    index = 0
    
    // ใส่เลขคู่ก่อน
    For i = 0 To length of A - 1 Do
        If A[i] % 2 == 0 Then
            result[index] = A[i]
            index = index + 1
        End If
    End For
    
    // ใส่เลขคี่ตามหลัง
    For i = 0 To length of A - 1 Do
        If A[i] % 2 != 0 Then
            result[index] = A[i]
            index = index + 1
        End If
    End For

    Return result
End Algorithm
```
---

## 3. ภาษา Java
import java.util.Arrays;

public class EvenOddRearranger {

    /**
     * อัลกอริทึมที่ 1: Recursive Two-Pointer
     * Base Case: left >= right
     * In-place Algorithm (แก้ไขข้อมูลในอาร์เรย์เดิม)
     */
    public static void rearrangeRecursive(int[] a, int left, int right) {
        if (a == null || left >= right) {
            return; // Base Case
        }

        if (a[left] % 2 == 0) {
            rearrangeRecursive(a, left + 1, right);
        } else if (a[right] % 2 != 0) {
            rearrangeRecursive(a, left, right - 1);
        } else {
            // สลับค่าระหว่างเลขคี่ด้านซ้าย และเลขคู่ด้านขวา
            int temp = a[left];
            a[left] = a[right];
            a[right] = temp;
            rearrangeRecursive(a, left + 1, right - 1);
        }
    }

    /**
     * อัลกอริทึมที่ 2: Iterative Two-Pointer
     * In-place Algorithm ใช้ลูปวนสลับข้อมูล
     */
    public static void rearrangeTwoPointer(int[] a) {
        if (a == null || a.length <= 1) return;

        int left = 0;
        int right = a.length - 1;

        while (left < right) {
            while (left < right && a[left] % 2 == 0) {
                left++;
            }
            while (left < right && a[right] % 2 != 0) {
                right--;
            }
            if (left < right) {
                int temp = a[left];
                a[left] = a[right];
                a[right] = temp;
                left++;
                right--;
            }
        }
    }

    /**
     * อัลกอริทึมที่ 3: Extra Array
     * สร้างอาร์เรย์ใหม่ รักษาสถานะ Stable Algorithm
     */
    public static int[] rearrangeExtraArray(int[] a) {
        if (a == null) return new int[0];

        int[] result = new int[a.length];
        int index = 0;

        // รอบที่ 1: ใส่เลขคู่
        for (int num : a) {
            if (num % 2 == 0) {
                result[index++] = num;
            }
        }

        // รอบที่ 2: ใส่เลขคี่
        for (int num : a) {
            if (num % 2 != 0) {
                result[index++] = num;
            }
        }

        return result;
    }

    public static void main(String[] args) {
        int[] input1 = {7, 2, 9, 4, 1, 6, 3, 8};
        System.out.println("Original Input: " + Arrays.toString(input1));

        // ทดสอบ Algorithm 1
        int[] arr1 = input1.clone();
        rearrangeRecursive(arr1, 0, arr1.length - 1);
        System.out.println("Recursive Two-Pointer: " + Arrays.toString(arr1));

        // ทดสอบ Algorithm 2
        int[] arr2 = input1.clone();
        rearrangeTwoPointer(arr2);
        System.out.println("Iterative Two-Pointer: " + Arrays.toString(arr2));

        // ทดสอบ Algorithm 3 ( Extra Array )
        int[] arr3 = rearrangeExtraArray(input1);
        System.out.println("Extra Array (Stable):  " + Arrays.toString(arr3));

        // ทดสอบการรักษาสภาพ Stable ด้วยข้อมูลเฉพาะ
        System.out.println("\n=== Testing Stability Check ===");
        int[] stableInput = {5, 2, 7, 4, 9, 6};
        System.out.println("Input:         " + Arrays.toString(stableInput));
        System.out.println("Two-Pointer:   " + Arrays.toString(rearrangeExtraArray(stableInput)));
    }
}
---

## 4. ข้อมูลน้ำเข้า&ออก
```
Input: [7, 2, 9, 4, 1, 6, 3, 8]
  - Recursive Two-Pointer Output: [8, 2, 6, 4, 1, 9, 3, 7] (Unstable)
  - Iterative Two-Pointer Output: [8, 2, 6, 4, 1, 9, 3, 7] (Unstable)
  - Extra Array Output:           [2, 4, 6, 8, 7, 9, 1, 3] (Stable)

Input (Stability Test): [5, 2, 7, 4, 9, 6]
  - Stable Output (Extra Array):  [2, 4, 6, 5, 7, 9]
```
---
## 5. วิเคราะห์ Time Complexity
### Recursive Two-Pointer: $O(n)$
* **เหตุผลสนับสนุน:** สแกนข้อมูลขยับตัวชี้ซ้าย/ขวาเข้าหากันรวม $n$ ครั้ง ทำงานสลับค่าคงที่ $O(1)$ รวมเป็น $O(n)$
### Iterative Two-Pointer: $O(n)$
* **เหตุผลสนับสนุน:** สแกนอาร์เรย์ด้วยลูป while ครอบคลุมสมาชิกทุกตัวเพียง 1 รอบ รวมเป็น $O(n)$
### Extra Array: $O(n)$
* **เหตุผลสนับสนุน:** วนลูปสแกนอาร์เรย์เดิม 2 รอบ ($2n$) ซึ่ง $O(2n) = O(n)$
---
## 6. วิเคราะห์ Space Complexity
### Recursive Two-Pointer: $O(n)$
* **เหตุผลสนับสนุน:** แม้จะเป็น In-place (ไม่สร้างอาร์เรย์ใหม่) แต่ใช้พื้นที่บน Call Stack สูงสุด $n$ ชั้น
### Iterative Two-Pointer: $O(1)$
* **เหตุผลสนับสนุน:** เป็น In-place Algorithm ที่แท้จริง ไม่ใช้ Call Stack และไม่ใช้หน่วยความจำเพิ่ม
### Extra Array: $O(n)$
* **เหตุผลสนับสนุน:** ต้องจองพื้นที่สร้างอาร์เรย์ผลลัพธ์ใหม่ขนาดเท่าอาร์เรย์เดิม ($n$)
---
## 7. วิเคราะห์เพิ่มเติม
### การเปลี่ยนแปลงอาร์เรย์เดิม:
* **Recursive / Iterative Two-Pointer:** เปลี่ยนแปลงอาร์เรย์เดิมทันที (In-place)
* **Extra Array:** ไม่แก้ไขอาร์เรย์เดิม แต่สร้างอาร์เรย์ใหม่คืนกลับไป

### ความเป็น Stable Algorithm:
* **Two-Pointer (ทั้ง 2 แบบ):** Unstable เนื่องจากทำการสลับข้ามตำแหน่งไกลๆ ทำให้ลำดับดั้งเดิมของสมาชิกภายในกลุ่มคู่และคี่เปลี่ยนแปลงไป
* **Extra Array:** Stable สามารถรักษาลำดับดั้งเดิมของสมาชิกไว้ได้ครบถ้วน เช่น Input [5, 2, 7, 4, 9, 6] จะได้ผลลัพธ์เป็น [2, 4, 6, 5, 7, 9]
---
### 8. การเปรียบเทียบข้อดี ข้อจำกัด และสรุปผล (สำหรับคัดลอก)

| ประเด็นการเปรียบเทียบ | อัลกอริทึมที่ 1: Recursive Two-Pointer | อัลกอริทึมที่ 2: Iterative Two-Pointer | อัลกอริทึมที่ 3: Extra Array |
| :--- | :--- | :--- | :--- |
| ข้อดี | โค้ดสั้น กระชับ ใช้แนวคิด In-place | ประสิทธิภาพสูงสุด ไม่เสียหน่วยความจำเพิ่ม (In-place) | เป็น Stable Algorithm รักษาลำดับเดิมของสมาชิกได้ |
| ข้อจำกัด | เสี่ยง Stack Overflow และเป็น Unstable | เป็น Unstable (ไม่รักษาลำดับเดิม) | ใช้ Space Complexity เพิ่มขึ้น O(n) |
| Time Complexity | O(n) | O(n) | O(n) |
| Space Complexity | O(n) | O(1) | O(n) |
| Stability | Unstable | Unstable | Stable |

สรุป: 
1. หากเน้นประสิทธิภาพสูงสุดและประหยัดหน่วยความจำ ให้เลือก **Iterative Two-Pointer** (In-place, Space O(1))
2. หากมีเงื่อนไขว่าต้องรักษาลำดับเดิมของสมาชิกไว้ด้วย (Stable Algorithm) ต้องเลือก **Extra Array** เท่านั้น
