# ข้อ 5: การแบ่งอาร์เรย์ตามค่า k (Partition Array Around Pivot k)

## 1. คำอธิบายแนวคิดของอัลกอริทึม

### โจทย์กำหนด
จัดเรียงสมาชิกในอาร์เรย์ $A$ ใหม่โดยใช้องค์ประกอบวิกฤต $k$ (Pivot) แบ่งออกเป็น 3 กลุ่ม:
1. **น้อยกว่า $k$** อยู่ด้านซ้ายสุด
2. **เท่ากับ $k$** อยู่ตรงกลาง
3. **มากกว่า $k$** อยู่ด้านขวาสุด

### อัลกอริทึมที่ 1: Three-Way Partitioning / Dutch National Flag (`partitionThreeWay`)
* **แนวคิด:** ใช้ตัวชี้ 3 ตัว ได้แก่ `low` (ขอบเขตกลุ่ม $< k$), `mid` (ตำแหน่งปัจจุบันที่กำลังตรวจ), และ `high` (ขอบเขตกลุ่ม $> k$)
* **ขั้นตอนการทำงาน:**
  * หาก $A[mid] < k$: สลับค่าระหว่าง $A[low]$ กับ $A[mid]$ จากนั้นเพิ่มค่า `low++` และ `mid++`
  * หาก $A[mid] == k$: เพิ่มค่า `mid++`
  * หาก $A[mid] > k$: สลับค่าระหว่าง $A[mid]$ กับ $A[high]$ จากนั้นลดค่า `high--` (โดยยังไม่เพิ่ม `mid` เพื่อตรวจค่าที่สลับมาใหม่)
* **ข้อดี:** ทำงานแบบ In-place วนสแกนอาร์เรย์เพียงรอบเดียว (Single Pass)

### อัลกอริทึมที่ 2: Extra Array Partitioning (`partitionExtraArray`)
* **แนวคิด:** สร้างอาร์เรย์ใหม่ขนาดเท่าเดิม สแกนอาร์เรย์เดิม 3 รอบ โดยรอบแรกเก็บเฉพาะค่าน้อยกว่า $k$ รอบสองเก็บค่าเท่ากับ $k$ และรอบสามเก็บค่ามากกว่า $k$
* **ข้อดี:** เข้าใจง่ายและเป็น Stable Algorithm (รักษาสภาพลำดับเดิมไว้ได้)

---

## 2. Pseudocode

```text
// 1. Three-Way Partitioning (Dutch National Flag Algorithm)
Algorithm partitionThreeWay(A, k)
    If A is null Then Return
    low = 0
    mid = 0
    high = length of A - 1
    
    While mid <= high Do
        If A[mid] < k Then
            Swap A[low] and A[mid]
            low = low + 1
            mid = mid + 1
        Else If A[mid] == k Then
            mid = mid + 1
        Else // A[mid] > k
            Swap A[mid] and A[high]
            high = high - 1
        End If
    End While
End Algorithm

// 2. Extra Array Partitioning
Algorithm partitionExtraArray(A, k)
    If A is null Then Return null
    Create array result of same size as A
    index = 0
    
    // รอบที่ 1: ใส่ค่าน้อยกว่า k
    For i = 0 To length of A - 1 Do
        If A[i] < k Then
            result[index] = A[i]
            index = index + 1
        End If
    End For
    
    // รอบที่ 2: ใส่ค่าเท่ากับ k
    For i = 0 To length of A - 1 Do
        If A[i] == k Then
            result[index] = A[i]
            index = index + 1
        End If
    End For
    
    // รอบที่ 3: ใส่ค่ามากกว่า k
    For i = 0 To length of A - 1 Do
        If A[i] > k Then
            result[index] = A[i]
            index = index + 1
        End If
    End For
    
    Return result
End Algorithm
```
---
## 3. ภาษา Java
import java.util.Arrays;

public class ArrayPartitioner {

    /**
     * อัลกอริทึมที่ 1: Three-Way Partitioning (Dutch National Flag)
     * In-place Algorithm สแกนรอบเดียว O(n)
     */
    public static void partitionThreeWay(int[] a, int k) {
        if (a == null || a.length <= 1) return;

        int low = 0;
        int mid = 0;
        int high = a.length - 1;

        while (mid <= high) {
            if (a[mid] < k) {
                int temp = a[low];
                a[low] = a[mid];
                a[mid] = temp;
                low++;
                mid++;
            } else if (a[mid] == k) {
                mid++;
            } else { // a[mid] > k
                int temp = a[mid];
                a[mid] = a[high];
                a[high] = temp;
                high--; // สลับค่าจากขวามา ต้องตรวจ mid ซ้ำในรอบถัดไป
            }
        }
    }

    /**
     * อัลกอริทึมที่ 2: Extra Array Partitioning
     * สร้างอาร์เรย์ใหม่ Stable Algorithm
     */
    public static int[] partitionExtraArray(int[] a, int k) {
        if (a == null) return new int[0];

        int[] result = new int[a.length];
        int index = 0;

        // รอบที่ 1: ค่าน้อยกว่า k
        for (int num : a) {
            if (num < k) result[index++] = num;
        }
        // รอบที่ 2: ค่าเท่ากับ k
        for (int num : a) {
            if (num == k) result[index++] = num;
        }
        // รอบที่ 3: ค่ามากกว่า k
        for (int num : a) {
            if (num > k) result[index++] = num;
        }

        return result;
    }

    public static void main(String[] args) {
        int[] input = {9, 12, 3, 5, 14, 10, 10, 2, 7, 10};
        int k = 10;

        System.out.println("Original Input: " + Arrays.toString(input) + " (Pivot k = " + k + ")");

        // ทดสอบ Algorithm 1: Three-Way Partitioning
        int[] arr1 = input.clone();
        partitionThreeWay(arr1, k);
        System.out.println("Three-Way Output:  " + Arrays.toString(arr1));

        // ทดสอบ Algorithm 2: Extra Array
        int[] arr2 = partitionExtraArray(input, k);
        System.out.println("Extra Array Output: " + Arrays.toString(arr2));
    }
}
---
## 4. ข้อมูลน้ำเข้า&ออก
```
Input: [9, 12, 3, 5, 14, 10, 10, 2, 7, 10], k = 10
  - Three-Way Output:  [9, 7, 3, 5, 2, 10, 10, 10, 14, 12] (Unstable)
  - Extra Array Output: [9, 3, 5, 2, 7, 10, 10, 10, 12, 14] (Stable)
```
---
## 5. วิเคราะห์ Time Complexity
### Three-Way Partitioning: $O(n)$
* **เหตุผลสนับสนุน:** สแกนข้อมูลโดยขยับตัวชี้ mid จาก 0 ถึง high ประมวลผลสมาชิกแต่ละตัวไม่เกิน 2 ครั้ง รวมเป็น $O(n)$
### Extra Array Partitioning: $O(n)$
* **เหตุผลสนับสนุน:** วนสแกนอาร์เรย์เดิม 3 รอบ ($3n$) ซึ่ง $O(3n) = O(n)$
---
## 6. วิเคราะห์ Space Complexity
### Three-Way Partitioning: $O(1)$
* **เหตุผลสนับสนุน:** เป็น In-place Algorithm สลับค่าภายในอาร์เรย์เดิมโดยไม่ใช้หน่วยความจำเพิ่ม
### Extra Array Partitioning: $O(n)$
* **เหตุผลสนับสนุน:** ต้องจองพื้นที่สร้างอาร์เรย์ผลลัพธ์ใหม่ขนาดเท่าเดิม $n$
---
## 7. วิเคราะห์เพิ่มเติม
### การเปรียบเทียบจำนวนการสแกน (Passes):
* **Three-Way Partitioning:** สแกนเพียง 1 รอบ (Single Pass)
* **Extra Array Partitioning:** สแกน 3 รอบ (Three Passes)
### ความเสถียร (Stability):
* **Three-Way Partitioning** เป็น Unstable Algorithm เนื่องจากมีการสลับค่าข้ามตำแหน่งไกลๆ ส่วนวิธี Extra Array เป็น Stable Algorithm
---
### 8. การเปรียบเทียบข้อดี ข้อจำกัด และสรุปผล (สำหรับคัดลอก)

| ประเด็นการเปรียบเทียบ | อัลกอริทึมที่ 1: Three-Way Partitioning | อัลกอริทึมที่ 2: Extra Array Partitioning |
| :--- | :--- | :--- |
| ข้อดี | ทำงานแบบ In-place (Space O(1)) สแกนข้อมูลเพียงรอบเดียว | รักษาสภาพลำดับดั้งเดิมของข้อมูลได้ (Stable Algorithm) |
| ข้อจำกัด | ไม่รักษาลำดับเดิมของสมาชิก (Unstable) | ต้องใช้หน่วยความจำสร้างอาร์เรย์ใหม่ O(n) และสแกน 3 รอบ |
| Time Complexity | O(n) | O(n) |
| Space Complexity | O(1) | O(n) |

สรุป: Three-Way Partitioning (Dutch National Flag Algorithm) เป็นตัวเลือกที่มีประสิทธิภาพสูงสุดเนื่องจากใช้เวลา O(n) และไม่ใช้หน่วยความจำเพิ่ม O(1)
