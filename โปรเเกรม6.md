# ข้อ 6: การค้นหาอาร์เรย์ย่อยที่มีผลรวมสูงสุด (Maximum Subarray Sum)

## 1. คำอธิบายแนวคิดของอัลกอริทึม

### โจทย์กำหนด
ค้นหาช่วงของสมาชิกต่อเนื่อง (Subarray) ในอาร์เรย์จำนวนเต็ม $A$ ที่มีผลรวมของสมาชิกสูงที่สุด

### อัลกอริทึมที่ 1: Brute Force (`maxSubarrayBruteForce`)
* **แนวคิด:** สร้างช่วง Subarray ที่เป็นไปได้ทั้งหมดโดยใช้ลูปซ้อนกัน 2 ชั้น
  * ลูปนอกกำหนดจุดเริ่มต้น `i`
  * ลูปในกำหนดจุดสิ้นสุด `j`
  * คำนวณผลรวมสะสมจาก `i` ถึง `j` แล้วอัปเดตค่าผลรวมสูงสุด (`maxSum`)
* **ข้อจำกัด:** สแกนตรวจสอบทุก Subarray ทำให้ทำงานช้า $O(n^2)$

### อัลกอริทึมที่ 2: Kadane's Algorithm (`maxSubarrayKadane`)
* **แนวคิด:** ใช้หลักการ **Dynamic Programming** ในการตัดสินใจ ณ สมาชิกแต่ละตัว ($A[i]$)
  * ค่าสะสมสูงสุด ณ ตำแหน่งปัจจุบัน (`currentMax`) จะเลือกระหว่าง: "นำ $A[i]$ ไปต่อกับผลรวมเดิม" หรือ "เริ่มนับ Subarray ใหม่ที่ $A[i]$"
  * สมการ: `currentMax = max(A[i], currentMax + A[i])`
  * คอยอัปเดตค่า `globalMax` สะสมไว้ตลอดการสแกน
* **ข้อดี:** สแกนข้อมูลเพียงรอบเดียว (Single Pass) ทำงานรวดเร็ว $O(n)$ และใช้พื้นที่ $O(1)$

---

## 2. Pseudocode

```text
// 1. Brute Force Algorithm
Algorithm maxSubarrayBruteForce(A)
    If A is null or empty Then Return 0
    maxSum = A[0]
    
    For i = 0 To length of A - 1 Do
        currentSum = 0
        For j = i To length of A - 1 Do
            currentSum = currentSum + A[j]
            If currentSum > maxSum Then
                maxSum = currentSum
            End If
        End For
    End For
    
    Return maxSum
End Algorithm

// 2. Kadane's Algorithm
Algorithm maxSubarrayKadane(A)
    If A is null or empty Then Return 0
    maxSum = A[0]
    currentMax = A[0]
    
    For i = 1 To length of A - 1 Do
        currentMax = Max(A[i], currentMax + A[i])
        maxSum = Max(maxSum, currentMax)
    End For
    
    Return maxSum
End Algorithm
```
---
## 3. ภาษา Java
public class MaxSubarray {

    /**
     * อัลกอริทึมที่ 1: Brute Force Approach
     * Time Complexity: O(n^2)
     * Space Complexity: O(1)
     */
    public static int maxSubarrayBruteForce(int[] a) {
        if (a == null || a.length == 0) return 0;

        int maxSum = a[0];

        for (int i = 0; i < a.length; i++) {
            int currentSum = 0;
            for (int j = i; j < a.length; j++) {
                currentSum += a[j];
                if (currentSum > maxSum) {
                    maxSum = currentSum;
                }
            }
        }
        return maxSum;
    }

    /**
     * อัลกอริทึมที่ 2: Kadane's Algorithm (Dynamic Programming)
     * Time Complexity: O(n)
     * Space Complexity: O(1)
     */
    public static int maxSubarrayKadane(int[] a) {
        if (a == null || a.length == 0) return 0;

        int maxSum = a[0];
        int currentMax = a[0];

        for (int i = 1; i < a.length; i++) {
            // เลือกสร้าง Subarray ใหม่ หรือต่อจาก Subarray เดิม
            currentMax = Math.max(a[i], currentMax + a[i]);
            // เก็บค่าผลรวมสูงสุดตลอดการสแกน
            maxSum = Math.max(maxSum, currentMax);
        }
        return maxSum;
    }

    public static void main(String[] args) {
        // 1. ชุดข้อมูลทดสอบปกติ (มีทั้งบวกและลบ)
        int[] input1 = {-2, 1, -3, 4, -1, 2, 1, -5, 4};
        System.out.println("Input 1: [-2, 1, -3, 4, -1, 2, 1, -5, 4]");
        System.out.println("  Brute Force Output: " + maxSubarrayBruteForce(input1));
        System.out.println("  Kadane Output:      " + maxSubarrayKadane(input1)); // คำตอบคือ 6 (ช่วง [4, -1, 2, 1])

        // 2. ชุดข้อมูลทดสอบกรณีเป็นลบทั้งหมด (All Negative Numbers)
        int[] input2 = {-8, -3, -6, -2, -5};
        System.out.println("\nInput 2 (All Negatives): [-8, -3, -6, -2, -5]");
        System.out.println("  Brute Force Output: " + maxSubarrayBruteForce(input2));
        System.out.println("  Kadane Output:      " + maxSubarrayKadane(input2)); // คำตอบคือ -2
    }
}
---
## 4. ข้อมูลน้ำเข้า&ออก
```
Input 1: [-2, 1, -3, 4, -1, 2, 1, -5, 4]
  - Subarray ที่ได้ผลรวมสูงสุด: [4, -1, 2, 1]
  - Max Sum Result: 6

Input 2 (All Negatives): [-8, -3, -6, -2, -5]
  - Subarray ที่ได้ผลรวมสูงสุด: [-2]
  - Max Sum Result: -2
```
---
## 5. วิเคราะห์ Time Complexity
### Brute Force Algorithm: $O(n^2)$
* **เหตุผลสนับสนุน:** มีการใช้ลูปซ้อนกัน 2 ชั้น ลูปนอกประมวลผล $n$ รอบ ลูปในประมวลผลเฉลี่ย $n/2$ รอบ รวมจำนวนรอบในการทำงานเป็น $n(n+1)/2$ ซึ่งเทียบเท่ากับ $O(n^2)$
* **Kadane's Algorithm:** $O(n)$เหตุผลสนับสนุน: วนสแกนอาร์เรย์เพียงรอบเดียวตั้งแต่ดัชนี 1 ถึง $n-1$ โดยแต่ละรอบทำการเปรียบเทียบค่าด้วย $O(1)$ รวมเป็น $O(n)$
---
## 6. วิเคราะห์ Space Complexity
### Brute Force Algorithm: $O(1)$
* **เหตุผลสนับสนุน:** ไม่มีการสร้างอาร์เรย์หรือ Call Stack เพิ่มเติม ใช้เพียงตัวแปรเก็บค่าผลรวมคงที่
### Kadane's Algorithm: $O(1)$
* **เหตุผลสนับสนุน:** ใช้เพียงตัวแปรท้องถิ่น currentMax และ maxSum ในการเก็บสถานะ ประหยัดหน่วยความจำระดับ $O(1)$
---
## 7. วิเคราะห์เพิ่มเติม
### กรณีที่ข้อมูลในอาร์เรย์เป็นลบทั้งหมด (All-Negative Arrays):
* **Kadane's Algorithm** ฉบับปรับปรุงนี้สามารถรับมือได้ถูกต้อง โดยจะเลือกค่าตัวเลขลบที่มีค่าน้อยที่สุด (ติดลบน้อยที่สุด) เป็นคำตอบ เช่น [-8, -3, -6, -2, -5] จะได้คำตอบเป็น -2
###การเปรียบเทียบกับ Divide and Conquer Approach ($O(n \log n)$):
* **วิธี** Divide and Conquer ต้องแบ่งอาร์เรย์ออกเป็นครึ่งซ้าย ครึ่งขวา และหาช่วงที่ข้ามจุดตรงกลาง (Crossing Subarray) ซึ่งต้องใช้ Space จาก Recursion Stack $O(\log n)$
* **Kadane's Algorithm** มีประสิทธิภาพเหนือกว่า ทั้งด้านเวลา ($O(n)$ vs $O(n \log n)$) และด้านหน่วยความจำ ($O(1)$ vs $O(\log n)$)
---

### 8. การเปรียบเทียบข้อดี ข้อจำกัด และสรุปผล (สำหรับคัดลอก)


| ประเด็นการเปรียบเทียบ | อัลกอริทึมที่ 1: Brute Force | อัลกอริทึมที่ 2: Kadane's Algorithm |
| :--- | :--- | :--- |
| ข้อดี | ตรงไปตรงมา เข้าใจง่าย และดึงช่วง Subarray ออกมาดูได้สะดวก | มีประสิทธิภาพสูงสุด สแกนข้อมูลเพียงรอบเดียว (Single Pass) |
| ข้อจำกัด | ทำงานช้ามากเมื่อข้อมูลมีขนาดใหญ่ (O(n^2)) | ต้องทำความเข้าใจแนวคิด Dynamic Programming |
| Time Complexity | O(n^2) | O(n) |
| Space Complexity | O(1) | O(1) |

สรุป: Kadane's Algorithm เป็นอัลกอริทึมที่ดีที่สุดในการแก้ปัญหา Maximum Subarray Sum ด้วย Time Complexity O(n) และ Space Complexity O(1)
